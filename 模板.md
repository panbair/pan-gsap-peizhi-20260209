确保了 className 的唯一性，避免了 GSAP 选择器之间的冲突。





你是专业前端程序员，专业gsap动画设计师，深入学习这个文件（src/views/web/index.vue）代码，开发更好的网站动画，没有思路就去gsap官网学习，如果开发学习图片，到这里取src/assets/image，也可以到网上取，比如实现滚动控制图片动画，文字动画，卡片动画等。再创作一些有创意的动画，每一个效果可以放一个组件,className要这之前的组件一样唯一性。




你是专业前端程序员，专业gsap动画设计师，深入学习这个文件（src/views/web/index.vue）代码，开发更好的网站动画，没有思路就去gsap官网学习，如果开发学习图片，到这里取src/assets/image，也可以到网上取。比如实现滚动控制动画，动画也可以借鉴（src/views/page2）。再创作一些有创意的动画，每一个效果可以放一个组件,className要这之前的组件一样唯一性。






你是专业前端程序员，专业gsap动画设计师，深入学习这个文件（src/views/web/index.vue）代码，开发更好的网站动画，没有思路就去gsap官网学习，如果开发需要图片，到这里取src/assets/image，也可以到网上取。比如实现滚动控制动画，动画也可以借鉴（src/views/page2）。再创作一些有创意的动画，每一个效果可以放一个组件,className要这之前的组件一样唯一性。




完美！我已经深入分析了项目代码结构。现在我将基于GSAP最佳实践和现有组件模式，创建一系列全新的创意动画组件。我将使用后缀164-175来确保唯一性。




## 💡 纯 CSS/GSAP 新创意组件推荐

以下组件**完全不需要 Canvas**,使用 CSS3 + GSAP 即可实现惊艳效果:

---

## 🌈 文字特效类

### 1. ScrollSplitReveal (分屏揭示)
**效果**: 文字从上下/左右两半切开,中间出现内容
```css
.word {
  display: inline-block;
  overflow: hidden;
  .char-top, .char-bottom {
    transform: translateY(-100%);
  }
}
```
**滚动**: 控制两部分向相反方向移动

### 2. ScrollTextScramble (文字解码)
**效果**: 像黑客解码一样,字符随机闪烁后定格
```javascript
const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
// 每次随机替换,逐步显示真实字符
```

### 3. ScrollWavyText (波浪文字)
**效果**: 文字像海浪一样上下起伏
```css
.char {
  animation-delay: calc(var(--i) * 0.1s);
}
```

### 4. ScrollTextShadow (文字阴影分离)
**效果**: 文字和 3D 阴影分离移动
```css
.text-shadow {
  transform: translateZ(50px) rotateX(20deg);
}
```

### 5. ScrollGradientText (流光文字)
**效果**: 渐变色文字流动
```css
.text {
  background-size: 200% auto;
  animation: gradientFlow 3s linear infinite;
}
```

### 6. ScrollOutlineText (轮廓描边)
**效果**: 文字只有轮廓,慢慢填充
```css
.outline-text {
  -webkit-text-stroke: 2px #fff;
  color: transparent;
}
```

### 7. ScrollTextMask (遮罩文字)
**效果**: 用图片/视频作为文字遮罩
```css
.masked-text {
  -webkit-background-clip: text;
  background-image: url('video.gif');
}
```

### 8. ScrollTextGlow (呼吸发光)
**效果**: 文字像呼吸一样闪烁发光
```css
.glow-text {
  animation: breathe 2s ease-in-out infinite;
}
```

---

## 🃏 卡片特效类

### 9. ScrollCardStack (卡片堆叠)
**效果**: 卡片从后方堆叠,滚动时展开
```css
.card:nth-child(n) {
  transform: translateY(calc(var(--i) * -20px));
}
```

### 10. ScrollCardZoom (卡片聚焦)
**效果**: 滚动时,当前卡片放大,其他缩小
```css
.card.active {
  transform: scale(1.2);
  z-index: 10;
}
```

### 11. ScrollCardRotate3D (3D 旋转卡片)
**效果**: 卡片 3D 空间旋转展示
```css
.card {
  transform: rotateY(var(--rotation));
  transform-style: preserve-3d;
}
```

### 12. ScrollCardBorder (边框生长)
**效果**: 卡片边框从四个角生长
```css
.border-corner {
  width: 0;
  height: 0;
  transition: 0.3s;
}
```

### 13. ScrollCardMorph (卡片变形)
**效果**: 卡片从圆形/方形/三角形变形
```css
.morph-card {
  border-radius: 50% → 0%;
}
```

### 14. ScrollCardGlass (玻璃态)
**效果**: 卡片毛玻璃 + 边框发光
```css
.glass-card {
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255,255,255,0.2);
}
```

### 15. ScrollCardRipple (水波纹)
**效果**: 点击/滚动产生水波纹扩散
```css
.ripple {
  animation: rippleEffect 1s ease-out;
}
```

---

## 🔄 变换特效类

### 16. ScrollShapeShift (形状变换)
**效果**: 正方形 → 圆形 → 三角形 → 星形
```css
.shape {
  clip-path: polygon(...) /* 动态改变 */
}
```

### 17. ScrollElasticScale (弹性缩放)
**效果**: 元素像橡皮筋一样弹性缩放
```javascript
gsap.to(el, { scale: 1.5, ease: 'elastic.out(1, 0.5)' })
```

### 18. ScrollPerspective (透视变换)
**效果:** 元素从远处飞向近处
```css
.perspective-container {
  perspective: 1000px;
}
```

### 19. ScrollFold (折叠效果)
**效果:** 像折纸一样折叠元素
```css
.fold-part {
  transform-origin: top;
  transform: rotateX(-90deg);
}
```

### 20. ScrollParallaxLayers (视差层级)
**效果:** 多层元素不同速度滚动
```javascript
gsap.to(layer, {
  y: scroll * speed,
  ease: 'none'
})
```

---

## 📊 图表特效类

### 21. ScrollChartGrow (图表生长)
**效果:** 柱状图/折线图从 0 生长
```css
.bar {
  transform: scaleY(0);
  transform-origin: bottom;
}
```

### 22. ScrollProgressCircle (环形进度)
**效果:** SVG 圆环进度条滚动增长
```css
.progress-circle {
  stroke-dasharray: 283;
  stroke-dashoffset: 283; /* → 0 */
}
```

### 23. ScrollDataReveal (数据揭示)
**效果:** 数字从 0 滚动到目标值
```javascript
gsap.to(number, {
  innerText: targetValue,
  snap: { innerText: 1 }
})
```

### 24. ScrollTimeline (时间线)
**效果:** 时间线逐步展开
```css
.timeline-item {
  opacity: 0;
  transform: translateX(-50px);
}
```

---

## 🖼️ 图片特效类

### 25. ScrollImageClip (图片裁剪)
**效果:** 图片从圆形/方形/自定义形状揭示
```css
.image-wrapper {
  clip-path: circle(0% at 50% 50%); /* → 100% */
}
```

### 26. ScrollImageBlur (模糊揭示)
**效果:** 图片从模糊变清晰
```css
.image {
  filter: blur(20px); /* → blur(0px) */
}
```

### 27. ScrollImageSepia (复古滤镜)
**效果:** 黑白 → 彩色 / 复古 → 现代
```css
.image {
  filter: sepia(100%); /* → 0% */
}
```

### 28. ScrollImageTilt (图片倾斜)
**效果:** 鼠标移动,图片跟随倾斜
```css
.image {
  transform: rotateX(var(--tiltX)) rotateY(var(--tiltY));
}
```

---

## 🎯 导航特效类

### 29. ScrollNavMorph (导航变形)
**效果:** 菜单项图标/文字变形
```css
.nav-item:hover .icon {
  transform: scale(1.2) rotate(45deg);
}
```

### 30. ScrollBreadcrumb (面包屑)
**效果:** 面包屑逐步显示
```css
.crumb {
  transform: translateX(-20px);
  opacity: 0;
}
```

### 31. ScrollTabSwitch (标签切换)
**效果:** 滑动指示器切换标签
```css
.indicator {
  width: 100px;
  left: var(--active-tab);
}
```

### 32. ScrollFloatingNav (悬浮导航)
**效果:** 导航栏随滚动浮起/下沉
```css
.nav {
  transform: translateY(var(--scroll) * -0.5);
}
```

---

## 🔤 排版特效类

### 33. ScrollTextFlow (文字流式)
**效果:** 文字像水流一样连续流动
```css
.marquee {
  animation: scrollText 10s linear infinite;
}
```

### 34. ScrollTextColumns (文字分栏)
**效果:** 文字从单列到多列展开
```css
.columns {
  column-count: 1; /* → 3 */
}
```

### 35. ScrollTextJustify (文字对齐)
**效果:** 左对齐 → 居中 → 右对齐
```css
.text {
  text-align: left; /* → center → right */
}
```

---

## 🌟 特殊特效类

### 36. ScrollCursorFollower (光标跟随)
**效果:** 元素跟随鼠标移动
```javascript
gsap.to(follower, {
  x: mouseX,
  y: mouseY,
  duration: 0.5
})
```

### 37. ScrollMagnetic (磁性吸附)
**效果:** 元素被鼠标磁性吸引
```javascript
gsap.to(el, {
  x: (mouseX - centerX) * 0.3,
  y: (mouseY - centerY) * 0.3
})
```

### 38. ScrollStickyReveal (粘性揭示)
**效果:** 元素粘性停留,然后揭示内容
```css
.sticky {
  position: sticky;
  top: 0;
}
```

### 39. ScrollParallaxCursor (视差光标)
**效果:** 多层元素以不同速度跟随光标
```css
.layer {
  transform: translate3d(var(--cursorX), var(--cursorY), 0);
}
```

### 40. ScrollScaleOnScroll (滚动缩放)
**效果:** 滚动时元素缩放
```css
.element {
  transform: scale(calc(1 + var(--scroll) * 0.001));
}
```

---

## 🎨 视觉特效类

### 41. ScrollGlitch (故障艺术)
**效果:** 故障风格的文字跳动
```css
.glitch {
  text-shadow: 2px 0 red, -2px 0 blue;
  animation: glitch 0.3s infinite;
}
```

### 42. ScrollInvert (反转色彩)
**效果:** 元素颜色反转
```css
.element {
  filter: invert(0); /* → 1 */
}
```

### 43. ScrollHueRotate (色相旋转)
**效果:** 颜色循环变化
```css
.element {
  filter: hue-rotate(0deg); /* → 360deg */
}
```

### 44. ScrollContrast (对比度变化)
**效果:** 低对比 → 高对比
```css
.element {
  filter: contrast(100%); /* → 200% */
}
```

---

## 📱 交互特效类

### 45. ScrollHoverReveal (悬停揭示)
**效果:** 滚动+悬停双重控制
```css
.card {
  opacity: var(--in-viewport);
  transform: translateY(var(--hover-state));
}
```

### 46. ScrollDragReveal (拖拽揭示)
**效果:** 拖拽滑动揭示内容
```css
.draggable {
  cursor: grab;
  transform: translateX(var(--drag-x));
}
```

### 47. ScrollPinchZoom (捏合缩放)
**效果:** 触摸设备捏合缩放
```javascript
gsap.to(el, {
  scale: touchDistance / initialDistance
})
```

### 48. ScrollSwipeUp (上滑揭示)
**效果:** 向上滑动手势触发
```css
.swipe-hint {
  animation: swipeUp 2s infinite;
}
```

---

## 🌙 主题特效类

### 49. ScrollThemeSwitch (主题切换)
**效果:** 滚动到某处切换深色/浅色
```css
body {
  background: var(--theme-bg);
  --theme-bg: #fff; /* → #000 */
}
```

### 50. ScrollAccentColor (强调色变化)
**效果:** 页面主色调随滚动变化
```css
:root {
  --accent: #ff0000; /* → #00ff00 → #0000ff */
}
```

---

## 🎯 最推荐开发的 10 个组件

| 排名 | 组件名 | 难度 | 视觉效果 | 开发价值 |
|------|--------|------|----------|----------|
| 1️⃣ | ScrollSplitReveal | ⭐⭐ | ⭐⭐⭐⭐⭐ | 高 - 简单但震撼 |
| 2️⃣ | ScrollTextScramble | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 高 - 科技感强 |
| 3️⃣ | ScrollCardZoom | ⭐⭐ | ⭐⭐⭐⭐ | 高 - 实用性强 |
| 4️⃣ | ScrollImageClip | ⭐⭐ | ⭐⭐⭐⭐ | 高 - 适配性好 |
| 5️⃣ | ScrollShapeShift | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中 - 创意独特 |
| 6️⃣ | ScrollGlitch | ⭐⭐ | ⭐⭐⭐⭐ | 中 - 风格鲜明 |
| 7️⃣ | ScrollProgressCircle | ⭐⭐ | ⭐⭐⭐ | 高 - 实用 |
| 8️⃣ | ScrollCardGlass | ⭐ | ⭐⭐⭐⭐ | 高 - 现代感 |
| 9️⃣ | ScrollWavyText | ⭐ | ⭐⭐⭐ | 高 - 简单有效 |
| 🔟 | ScrollMagnetic | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中 - 交互性强 |

---

需要我帮你实现其中某个组件吗? 我可以生成完整的代码!
## 💡 纯 CSS/GSAP 新创意组件推荐

以下组件**完全不需要 Canvas**,使用 CSS3 + GSAP 即可实现惊艳效果:

---

## 🌈 文字特效类

### 1. ScrollSplitReveal (分屏揭示)
**效果**: 文字从上下/左右两半切开,中间出现内容
```css
.word {
  display: inline-block;
  overflow: hidden;
  .char-top, .char-bottom {
    transform: translateY(-100%);
  }
}
```
**滚动**: 控制两部分向相反方向移动

### 2. ScrollTextScramble (文字解码)
**效果**: 像黑客解码一样,字符随机闪烁后定格
```javascript
const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789'
// 每次随机替换,逐步显示真实字符
```

### 3. ScrollWavyText (波浪文字)
**效果**: 文字像海浪一样上下起伏
```css
.char {
  animation-delay: calc(var(--i) * 0.1s);
}
```

### 4. ScrollTextShadow (文字阴影分离)
**效果**: 文字和 3D 阴影分离移动
```css
.text-shadow {
  transform: translateZ(50px) rotateX(20deg);
}
```

### 5. ScrollGradientText (流光文字)
**效果**: 渐变色文字流动
```css
.text {
  background-size: 200% auto;
  animation: gradientFlow 3s linear infinite;
}
```

### 6. ScrollOutlineText (轮廓描边)
**效果**: 文字只有轮廓,慢慢填充
```css
.outline-text {
  -webkit-text-stroke: 2px #fff;
  color: transparent;
}
```

### 7. ScrollTextMask (遮罩文字)
**效果**: 用图片/视频作为文字遮罩
```css
.masked-text {
  -webkit-background-clip: text;
  background-image: url('video.gif');
}
```

### 8. ScrollTextGlow (呼吸发光)
**效果**: 文字像呼吸一样闪烁发光
```css
.glow-text {
  animation: breathe 2s ease-in-out infinite;
}
```

---

## 🃏 卡片特效类

### 9. ScrollCardStack (卡片堆叠)
**效果**: 卡片从后方堆叠,滚动时展开
```css
.card:nth-child(n) {
  transform: translateY(calc(var(--i) * -20px));
}
```

### 10. ScrollCardZoom (卡片聚焦)
**效果**: 滚动时,当前卡片放大,其他缩小
```css
.card.active {
  transform: scale(1.2);
  z-index: 10;
}
```

### 11. ScrollCardRotate3D (3D 旋转卡片)
**效果**: 卡片 3D 空间旋转展示
```css
.card {
  transform: rotateY(var(--rotation));
  transform-style: preserve-3d;
}
```

### 12. ScrollCardBorder (边框生长)
**效果**: 卡片边框从四个角生长
```css
.border-corner {
  width: 0;
  height: 0;
  transition: 0.3s;
}
```

### 13. ScrollCardMorph (卡片变形)
**效果**: 卡片从圆形/方形/三角形变形
```css
.morph-card {
  border-radius: 50% → 0%;
}
```

### 14. ScrollCardGlass (玻璃态)
**效果**: 卡片毛玻璃 + 边框发光
```css
.glass-card {
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255,255,255,0.2);
}
```

### 15. ScrollCardRipple (水波纹)
**效果**: 点击/滚动产生水波纹扩散
```css
.ripple {
  animation: rippleEffect 1s ease-out;
}
```

---

## 🔄 变换特效类

### 16. ScrollShapeShift (形状变换)
**效果**: 正方形 → 圆形 → 三角形 → 星形
```css
.shape {
  clip-path: polygon(...) /* 动态改变 */
}
```

### 17. ScrollElasticScale (弹性缩放)
**效果**: 元素像橡皮筋一样弹性缩放
```javascript
gsap.to(el, { scale: 1.5, ease: 'elastic.out(1, 0.5)' })
```

### 18. ScrollPerspective (透视变换)
**效果:** 元素从远处飞向近处
```css
.perspective-container {
  perspective: 1000px;
}
```

### 19. ScrollFold (折叠效果)
**效果:** 像折纸一样折叠元素
```css
.fold-part {
  transform-origin: top;
  transform: rotateX(-90deg);
}
```

### 20. ScrollParallaxLayers (视差层级)
**效果:** 多层元素不同速度滚动
```javascript
gsap.to(layer, {
  y: scroll * speed,
  ease: 'none'
})
```

---

## 📊 图表特效类

### 21. ScrollChartGrow (图表生长)
**效果:** 柱状图/折线图从 0 生长
```css
.bar {
  transform: scaleY(0);
  transform-origin: bottom;
}
```

### 22. ScrollProgressCircle (环形进度)
**效果:** SVG 圆环进度条滚动增长
```css
.progress-circle {
  stroke-dasharray: 283;
  stroke-dashoffset: 283; /* → 0 */
}
```

### 23. ScrollDataReveal (数据揭示)
**效果:** 数字从 0 滚动到目标值
```javascript
gsap.to(number, {
  innerText: targetValue,
  snap: { innerText: 1 }
})
```

### 24. ScrollTimeline (时间线)
**效果:** 时间线逐步展开
```css
.timeline-item {
  opacity: 0;
  transform: translateX(-50px);
}
```

---

## 🖼️ 图片特效类

### 25. ScrollImageClip (图片裁剪)
**效果:** 图片从圆形/方形/自定义形状揭示
```css
.image-wrapper {
  clip-path: circle(0% at 50% 50%); /* → 100% */
}
```

### 26. ScrollImageBlur (模糊揭示)
**效果:** 图片从模糊变清晰
```css
.image {
  filter: blur(20px); /* → blur(0px) */
}
```

### 27. ScrollImageSepia (复古滤镜)
**效果:** 黑白 → 彩色 / 复古 → 现代
```css
.image {
  filter: sepia(100%); /* → 0% */
}
```

### 28. ScrollImageTilt (图片倾斜)
**效果:** 鼠标移动,图片跟随倾斜
```css
.image {
  transform: rotateX(var(--tiltX)) rotateY(var(--tiltY));
}
```

---

## 🎯 导航特效类

### 29. ScrollNavMorph (导航变形)
**效果:** 菜单项图标/文字变形
```css
.nav-item:hover .icon {
  transform: scale(1.2) rotate(45deg);
}
```

### 30. ScrollBreadcrumb (面包屑)
**效果:** 面包屑逐步显示
```css
.crumb {
  transform: translateX(-20px);
  opacity: 0;
}
```

### 31. ScrollTabSwitch (标签切换)
**效果:** 滑动指示器切换标签
```css
.indicator {
  width: 100px;
  left: var(--active-tab);
}
```

### 32. ScrollFloatingNav (悬浮导航)
**效果:** 导航栏随滚动浮起/下沉
```css
.nav {
  transform: translateY(var(--scroll) * -0.5);
}
```

---

## 🔤 排版特效类

### 33. ScrollTextFlow (文字流式)
**效果:** 文字像水流一样连续流动
```css
.marquee {
  animation: scrollText 10s linear infinite;
}
```

### 34. ScrollTextColumns (文字分栏)
**效果:** 文字从单列到多列展开
```css
.columns {
  column-count: 1; /* → 3 */
}
```

### 35. ScrollTextJustify (文字对齐)
**效果:** 左对齐 → 居中 → 右对齐
```css
.text {
  text-align: left; /* → center → right */
}
```

---

## 🌟 特殊特效类

### 36. ScrollCursorFollower (光标跟随)
**效果:** 元素跟随鼠标移动
```javascript
gsap.to(follower, {
  x: mouseX,
  y: mouseY,
  duration: 0.5
})
```

### 37. ScrollMagnetic (磁性吸附)
**效果:** 元素被鼠标磁性吸引
```javascript
gsap.to(el, {
  x: (mouseX - centerX) * 0.3,
  y: (mouseY - centerY) * 0.3
})
```

### 38. ScrollStickyReveal (粘性揭示)
**效果:** 元素粘性停留,然后揭示内容
```css
.sticky {
  position: sticky;
  top: 0;
}
```

### 39. ScrollParallaxCursor (视差光标)
**效果:** 多层元素以不同速度跟随光标
```css
.layer {
  transform: translate3d(var(--cursorX), var(--cursorY), 0);
}
```

### 40. ScrollScaleOnScroll (滚动缩放)
**效果:** 滚动时元素缩放
```css
.element {
  transform: scale(calc(1 + var(--scroll) * 0.001));
}
```

---

## 🎨 视觉特效类

### 41. ScrollGlitch (故障艺术)
**效果:** 故障风格的文字跳动
```css
.glitch {
  text-shadow: 2px 0 red, -2px 0 blue;
  animation: glitch 0.3s infinite;
}
```

### 42. ScrollInvert (反转色彩)
**效果:** 元素颜色反转
```css
.element {
  filter: invert(0); /* → 1 */
}
```

### 43. ScrollHueRotate (色相旋转)
**效果:** 颜色循环变化
```css
.element {
  filter: hue-rotate(0deg); /* → 360deg */
}
```

### 44. ScrollContrast (对比度变化)
**效果:** 低对比 → 高对比
```css
.element {
  filter: contrast(100%); /* → 200% */
}
```

---

## 📱 交互特效类

### 45. ScrollHoverReveal (悬停揭示)
**效果:** 滚动+悬停双重控制
```css
.card {
  opacity: var(--in-viewport);
  transform: translateY(var(--hover-state));
}
```

### 46. ScrollDragReveal (拖拽揭示)
**效果:** 拖拽滑动揭示内容
```css
.draggable {
  cursor: grab;
  transform: translateX(var(--drag-x));
}
```

### 47. ScrollPinchZoom (捏合缩放)
**效果:** 触摸设备捏合缩放
```javascript
gsap.to(el, {
  scale: touchDistance / initialDistance
})
```

### 48. ScrollSwipeUp (上滑揭示)
**效果:** 向上滑动手势触发
```css
.swipe-hint {
  animation: swipeUp 2s infinite;
}
```

---

## 🌙 主题特效类

### 49. ScrollThemeSwitch (主题切换)
**效果:** 滚动到某处切换深色/浅色
```css
body {
  background: var(--theme-bg);
  --theme-bg: #fff; /* → #000 */
}
```

### 50. ScrollAccentColor (强调色变化)
**效果:** 页面主色调随滚动变化
```css
:root {
  --accent: #ff0000; /* → #00ff00 → #0000ff */
}
```

---

## 🎯 最推荐开发的 10 个组件

| 排名 | 组件名 | 难度 | 视觉效果 | 开发价值 |
|------|--------|------|----------|----------|
| 1️⃣ | ScrollSplitReveal | ⭐⭐ | ⭐⭐⭐⭐⭐ | 高 - 简单但震撼 |
| 2️⃣ | ScrollTextScramble | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 高 - 科技感强 |
| 3️⃣ | ScrollCardZoom | ⭐⭐ | ⭐⭐⭐⭐ | 高 - 实用性强 |
| 4️⃣ | ScrollImageClip | ⭐⭐ | ⭐⭐⭐⭐ | 高 - 适配性好 |
| 5️⃣ | ScrollShapeShift | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中 - 创意独特 |
| 6️⃣ | ScrollGlitch | ⭐⭐ | ⭐⭐⭐⭐ | 中 - 风格鲜明 |
| 7️⃣ | ScrollProgressCircle | ⭐⭐ | ⭐⭐⭐ | 高 - 实用 |
| 8️⃣ | ScrollCardGlass | ⭐ | ⭐⭐⭐⭐ | 高 - 现代感 |
| 9️⃣ | ScrollWavyText | ⭐ | ⭐⭐⭐ | 高 - 简单有效 |
| 🔟 | ScrollMagnetic | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 中 - 交互性强 |

---

需要我帮你实现其中某个组件吗? 我可以生成完整的代码!