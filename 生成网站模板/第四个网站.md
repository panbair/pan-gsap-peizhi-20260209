你是一位专业的前端开发工程师、UI/UX设计师和动画专家，精通以下技术栈：

**核心技术栈：**
- 前端框架：HTML5 + JavaScript ES6+（纯原生实现）
- 动画引擎：GSAP 3.12+（含 ScrollTrigger + ScrollToPlugin + MotionPathPlugin）
- 构建工具：无需构建工具，单文件 HTML 部署
- 样式方案：CSS3 + Flexbox + Grid + CSS Variables + Backdrop Filter
- 图片资源：Unsplash（高质量免费图库）

**你的核心能力：**
✅ 创建流畅的 60fps 动画效果
✅ 精通 GSAP 的 ScrollTrigger 滚动控制
✅ 擅长响应式设计和性能优化
✅ 注重用户体验和视觉设计
✅ GPU 加速优化，避免性能瓶颈
✅ 无障碍访问和 SEO 友好

---

## 📋 项目要求

### 主题定位
创建一个以「智能手机」为主题的科技产品官网，融合现代科技美学与流畅交互体验，打造具有未来感的手机品牌形象。

### 页面结构
1. **导航栏** - 固定顶部，Logo + 导航链接，毛玻璃效果，滚动样式变化
2. **首页 Hero** - 手机产品展示，震撼视觉冲击力，3D 悬浮效果
3. **产品特性** - 4-5 个核心特性卡片，磁力交互 + 霓虹光效
4. **性能参数** - 数据统计展示，MotionPath 路径动画
5. **产品图库** - 6 张高清图片，MorphGrid 变形网格 + ImageComparison 对比
6. **用户体验** - 4 个体验场景，MorphingCards 变形翻转
7. **技术规格** - ModernCards 现代卡片展示
8. **客户评价** - HorizontalScroll 横向滚动
9. **联系我们** - 联系信息 + 在线表单 + MinimalistCards 极简设计
10. **页脚** - 完整的网站导航和版权信息 + InfiniteScrollText 无限滚动文字

### 底层动画模板（必须使用）
**参考位置：** `pan-gsap-peizhi-20260209/src/views/template-read`

#### 可选模板列表（共53种）：
**基础动画类：**
1. `customizeAnimation` - 横向模版（简单平移）
2. `initHorizontalAnimation` - 横竖模版（混合横竖屏）
3. `infinitePanelScrollAnimation` - 无限模版（无限循环）
4. `scalePanelScrollAnimation` - 缩放模版（缩放效果）
5. `smoothHorizontalScroll` - 平滑横向
6. `staggerHorizontal` - 交错横向
7. `scaleFadeIn` - 缩放淡入
8. `infiniteRotation` - 无限旋转
9. `smoothCombination` - 平滑组合
10. `fadeUp` - 淡入上移
11. `elasticScale` - 弹性缩放
12. `infiniteLoopOptimization` - 无限循环优化

**3D效果类：**
13. `threeDCardFlipAnimation` - 🃏 3D卡片翻转
14. `magneticAttraction` - 磁力吸引

**粒子特效类：**
15. `particleWaveAnimation` - 粒子波浪（推荐用于手机主题）
16. `particleConvergence` - 粒子汇聚
17. `particleExplosion` - 粒子爆炸

**科技感类：**
18. `wormholeTravel` - 虫洞穿梭
19. `quantumEntanglement` - 量子纠缠
20. `magneticDistortion` - 磁场扭曲
21. `auroraFlow` - 极光流淌（推荐用于手机主题）
22. `holographicScan` - 全息扫描
23. `kaleidoscopeMirror` - 万花筒镜像
24. `electromagneticWave` - 电磁波动
25. `matrixCodeRain` - 矩阵代码雨
26. `galaxyRotation` - 星系旋转
27. `nebulaBirth` - 星云诞生
28. `neonCity` - 霓虹城市
29. `cyberSpace` - 赛博空间
30. `holographicDataStream` - 全息数据流
31. `neonGlitch` - 霓虹故障
32. `wormholeCross` - 虫洞穿越

**视觉特效类：**
33. `glassShatter` - 玻璃破碎
34. `liquidFlow` - 液体流动
35. `paperFold` - 纸张折叠
36. `mobiusLoop` - 莫比乌斯环
37. `honeycombUnfold` - 蜂巢展开
38. `liquidTension` - 液体张力
39. `fluidVortex` - 流体涡旋
40. `panoramaUnfold` - 全景展开
41. `interstellarPortal` - 星际传送门
42. `liquidMetal` - 液态金属
43. `timeFolding` - 时间折叠
44. `parallaxExtreme` - 极限视差
45. `liquidMorphing` - 液态变形
46. `origamiUnfold` - 折纸展开
47. `auroraRipple` - 极光波纹

**高级特效类：**
48. `fractalGrowth` - 专业 🌸 分形生长
49. `dnaDoubleHelix` - DNA双螺旋
50. `fragmentReassembly` - 碎片重组
51. `multiLayerParallax` - 多层视差
52. `quantumTimeTravel` - 量子时空穿越

---

#### 🎯 推荐使用方案（按手机主题匹配度排序）：

**方案一：粒子波浪（强烈推荐 ⭐⭐⭐⭐⭐）**
- **模板名称：** `particleWaveAnimation`（粒子波浪）
- **templateView:** `particleWaveAnimation`
- **核心动画元素：**
  - 40-60 个浮动粒子，科技蓝色调
  - 3 层科技波浪背景动画（渐变 + 模糊）
  - 面板波浪式入场动画（y + scale + rotation + 3D perspective）
  - 持续循环的背景动画（呼吸效果 + 光效流动）
- **推荐理由：** 粒子效果完美匹配手机科技主题，蓝调科技感强烈，背景流动效果不抢眼

**方案二：极光流淌（推荐 ⭐⭐⭐⭐）**
- **模板名称：** `auroraFlow`（极光流淌）
- **templateView:** `auroraFlow`
- **核心动画元素：**
  - 多层极光波浪动画
  - 渐变流动背景
  - 平滑过渡效果
  - 持续循环的光效流动
- **推荐理由：** 极光效果营造高端科技感，蓝绿色调适合手机主题

**方案三：磁力吸引（推荐 ⭐⭐⭐⭐）**
- **模板名称：** `magneticAttraction`（磁力吸引）
- **templateView:** `magneticAttraction`
- **核心动画元素：**
  - 鼠标磁力跟随效果
  - 3D 旋转交互
  - 粒子环绕动画
  - 动态引力场
- **推荐理由：** 磁力交互效果完美匹配产品特性展示区域

**方案四：全息扫描（推荐 ⭐⭐⭐）**
- **模板名称：** `holographicScan`（全息扫描）
- **templateView:** `holographicScan`
- **核心动画元素：**
  - 全息扫描线动画
  - 蓝色光束效果
  - 扫描揭示内容
  - 科技感强烈
- **推荐理由：** 全息效果完美适配产品规格和技术展示区域

**方案五：霓虹城市（推荐 ⭐⭐⭐）**
- **模板名称：** `neonCity`（霓虹城市）
- **templateView:** `neonCity`
- **核心动画元素：**
  - 霓虹光效背景
  - 城市剪影流动
  - 动态光效闪烁
  - 赛博朋克风格
- **推荐理由：** 霓虹效果强烈，适合营造未来科技氛围

---

#### ⚠️ 使用说明：

**必须遵循：**
- ❌ 不能直接引入 template-read 文件
- ✅ 复制代码并根据需求优化调整
- ✅ 保持核心动画逻辑，优化视觉效果为科技蓝
- ✅ 所有动画必须服务于内容

**选择建议：**
- 对于 **手机产品官网**，强烈推荐使用 `particleWaveAnimation`（粒子波浪）
- 如果想要 **更强的交互感**，可以在产品特性区域叠加 `magneticAttraction`（磁力吸引）
- 如果想要 **高端大气感**，可以使用 `auroraFlow`（极光流淌）
- 可以 **组合使用多个模板**，不同区块使用不同动画效果

**默认推荐：** 本模板默认使用 `particleWaveAnimation`（粒子波浪）作为底层动画模板。

### 参考组件动画效果
**参考位置：** `pan-gsap-peizhi-20260209/src/views/web`
**需参考的组件（复制并优化）：**

#### 1. ScrollMagneticGrid - 磁力网格
**应用场景：** 产品特性展示
**核心效果：**
- 鼠标跟随磁力效果（3D 旋转 + 缩放）
- 玻璃态卡片（backdrop-filter: blur）
- 从中心扩散的入场动画
- 3D 视差效果（perspective: 2000px）

**动画代码：**
```javascript
// 磁力交互
const handleMagneticMove = (event, item) => {
  const rect = item.getBoundingClientRect();
  const centerX = rect.left + rect.width / 2;
  const centerY = rect.top + rect.height / 2;
  const moveX = (event.clientX - centerX) / rect.width * 30;
  const moveY = (event.clientY - centerY) / rect.height * 30;

  gsap.to(item, {
    x: moveX,
    y: moveY,
    rotateX: moveY * -0.5,
    rotateY: moveX * 0.5,
    duration: 0.3,
    ease: 'power2.out'
  });
};

// 入场动画
gsap.from(gridItems, {
  y: 100,
  opacity: 0,
  scale: 0.8,
  duration: 0.8,
  ease: 'back.out(1.5)',
  stagger: { each: 0.08, from: 'center' },
  scrollTrigger: {
    trigger: gridItems[0],
    start: 'top 85%',
    toggleActions: 'play none none reverse'
  }
});
```

#### 2. ScrollNeonEclipseCards - 霓虹卡片
**应用场景：** 产品特性悬停效果
**核心效果：**
- 多层光效（Aura + Glow + Eclipse）
- 3D 旋转跟随鼠标（rotateX/rotateY）
- 粒子系统环绕动画
- 滚动控制发光强度

**动画代码：**
```javascript
// 霓虹悬停效果
const handleCardHover = (event, card) => {
  const rect = card.getBoundingClientRect();
  const x = ((event.clientX - rect.left) / rect.width) * 2 - 1;
  const y = ((event.clientY - rect.top) / rect.height) * 2 - 1;

  gsap.to(card, {
    rotateY: x * 12,
    rotateX: -y * 12,
    scale: 1.05,
    duration: 0.4,
    ease: 'power2.out'
  });

  // 霓虹光效增强
  gsap.to(card.querySelector('.neon-aura'), {
    opacity: 0.6,
    scale: 1.2,
    duration: 0.4
  });
};

// 滚动控制光效
ScrollTrigger.create({
  trigger: '.neon-cards-section',
  start: 'top 80%',
  end: 'bottom 20%',
  onUpdate: (self) => {
    const progress = self.progress;
    cards.forEach(card => {
      const aura = card.querySelector('.neon-aura');
      gsap.to(aura, {
        opacity: 0.3 + progress * 0.4,
        scale: 1 + progress * 0.3
      });
    });
  }
});
```

#### 3. MotionPathCards - 运动路径
**应用场景：** 性能参数数据流动
**核心效果：**
- SVG 路径运动（motionPath）
- 自动旋转（autoRotate: true）
- 滚动控制运动进度
- 速度切换（timeScale）

**动画代码：**
```javascript
// 必须注册 MotionPathPlugin
gsap.registerPlugin(MotionPathPlugin);

// 路径运动动画
const motionTween = gsap.to('.motion-dot', {
  motionPath: {
    path: '#motionPath1',
    align: '#motionPath1',
    alignOrigin: [0.5, 0.5],
    autoRotate: true
  },
  duration: 3,
  ease: 'power1.inOut',
  repeat: -1,
  yoyo: true
});

// 滚动控制
gsap.timeline({
  scrollTrigger: {
    trigger: '.motion-path-section',
    start: 'top bottom',
    end: 'bottom top',
    scrub: 1
  }
}).to('.motion-dot', {
  motionPath: {
    path: '#motionPath1',
    align: '#motionPath1',
    alignOrigin: [0.5, 0.5],
    autoRotate: true
  },
  duration: 1.5,
  stagger: 0.1,
  ease: 'none'
});
```

#### 4. MorphGrid - 变形网格
**应用场景：** 产品图库展示
**核心效果：**
- 从圆形变形为方形
- 卡片展开全屏效果
- 点击切换展开状态
- 滚动触发变形动画

**动画代码：**
```javascript
// 网格变形入场
gsap.fromTo(gridCells, {
  scale: 0.5,
  opacity: 0,
  borderRadius: '50%'
}, {
  scale: 1,
  opacity: 1,
  borderRadius: '16px',
  duration: 1,
  ease: 'back.out(1.5)',
  stagger: 0.1,
  scrollTrigger: {
    trigger: '.morph-grid-section',
    start: 'top 85%',
    toggleActions: 'play none none reverse'
  }
});

// 展开动画
const expandCell = (cell) => {
  gsap.to(cell, {
    position: 'fixed',
    top: '50%',
    left: '50%',
    width: '80vw',
    height: '70vh',
    zIndex: 100,
    transform: 'translate(-50%, -50%)',
    duration: 0.6,
    ease: 'power3.out'
  });
};
```

#### 5. ImageComparison - 图片对比
**应用场景：** 产品前后对比
**核心效果：**
- 实时拖动对比
- GSAP 控制滑块位置
- 自动播放切换
- 鼠标悬停暂停

**动画代码：**
```javascript
// 图片对比
const handleMouseMove = (event, wrapper) => {
  const rect = wrapper.getBoundingClientRect();
  const x = event.clientX - rect.left;
  const percentage = Math.max(0, Math.min(100, (x / rect.width) * 100));

  gsap.to(wrapper.querySelector('.comparison-slider'), {
    left: percentage + '%',
    duration: 0.1,
    ease: 'none'
  });

  gsap.to(wrapper.querySelector('.comparison-overlay'), {
    width: percentage + '%',
    duration: 0.1,
    ease: 'none'
  });
};
```

#### 6. ScrollMorphingCards - 变形卡片
**应用场景：** 用户体验场景
**核心效果：**
- 3D 翻转动画（rotateY: 180deg）
- 悬停缩放 + 阴影
- 正反面独立内容
- backface-visibility 隐藏背面

**动画代码：**
```javascript
// 3D 翻转动画
const handleCardHover = (card) => {
  const cardInner = card.querySelector('.card-inner');

  gsap.to(cardInner, {
    rotateY: 180,
    duration: 0.8,
    ease: 'back.out(1.5)'
  });

  gsap.to(card, {
    scale: 1.05,
    boxShadow: '0 30px 60px rgba(59, 130, 246, 0.3)',
    duration: 0.5,
    ease: 'power2.out'
  });
};
```

#### 7. HorizontalScroll - 横向滚动
**应用场景：** 客户评价展示
**核心效果：**
- Pin 固定滚动区域
- Snap 自动吸附
- 渐变叠加光效
- xPercent 控制滚动

**动画代码：**
```javascript
// 横向滚动
const sections = gsap.utils.toArray('.horizontal-scroll-item');

gsap.to(sections, {
  xPercent: -100 * (sections.length - 1),
  ease: 'none',
  scrollTrigger: {
    trigger: '.horizontal-scroll-track',
    pin: true,
    scrub: 1,
    snap: 1 / (sections.length - 1),
    end: () => '+=' + track.offsetWidth
  }
});
```

#### 8. InfiniteScrollText - 无限滚动文字
**应用场景：** 页脚装饰 + 标题背景
**核心效果：**
- 无缝循环动画
- 反向滚动
- 滚动加速控制
- 渐变文字效果

**动画代码：**
```javascript
// 无限滚动文字
tracks.forEach((track, index) => {
  const direction = index % 2 === 0 ? 1 : -1;
  const text = track.querySelector('.scroll-text');
  const textWidth = text.offsetWidth;

  const tween = gsap.to(track, {
    x: direction * -textWidth / 2,
    duration: 15 + index * 5,
    repeat: -1,
    ease: 'none'
  });
});

// 滚动加速
ScrollTrigger.create({
  trigger: '.infinite-scroll-section',
  start: 'top bottom',
  end: 'bottom top',
  scrub: 1.5,
  onUpdate: (self) => {
    scrollTweens.forEach(tween => {
      tween.timeScale(1 + self.progress * 3);
    });
  }
});
```

**重要提示：**
- ❌ 不能直接引用这些 Vue 组件
- ✅ 提取动画逻辑，用原生 JS + GSAP 重写
- ✅ 保持动画效果的真实性和流畅度
- ✅ 根据手机主题优化动画参数（蓝色调、科技感）

---

## 🎨 设计要求

### 视觉风格
**整体风格：** 未来科技 + 现代简约 + 流畅交互
**配色方案：**
- 主色调：科技蓝 (#3b82f6)
- 辅助色：深空黑 (#0f172a) + 纯白 (#ffffff)
- 强调色：霓虹紫 (#8b5cf6) + 赛博青 (#06b6d4)
- 背景色：深色渐变 (#0f172a → #1e293b → #0f172a)
- 文字色：纯白 (#ffffff) + 浅灰 (#94a3b8)

**渐变方案：**
```css
/* 主渐变 */
--gradient-primary: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
--gradient-dark: linear-gradient(180deg, #0f172a 0%, #1e293b 50%, #0f172a 100%);
--gradient-neon: linear-gradient(90deg, #06b6d4, #3b82f6, #8b5cf6, #06b6d4);
```

**玻璃态效果：**
```css
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
}
```

**排版原则：**
- ✅ 大标题使用超大字号（clamp 4rem - 8rem）
- ✅ 正文简洁有力，避免冗长
- ✅ 使用高质量手机产品图片
- ✅ 字体：标题用 Inter/Poppins，正文用 SF Pro/系统字体
- ✅ 字间距宽松，营造高端感

### 动画设计原则
**必须遵循：**
1. ✅ 所有元素初始 `opacity: 1`，避免内容不可见
2. ✅ 滚动触发动画 + 持续动画结合
3. ✅ 使用 `toggleActions: 'play none none reverse'` 实现反向播放
4. ✅ 离开视窗时自动反向播放动画
5. ✅ 使用 GPU 加速属性（transform, will-change, backface-visibility）
6. ✅ 使用 3D transform 增强深度感
7. ✅ 磁力交互跟随鼠标移动
8. ✅ 霓虹光效营造科技感

**标准 ScrollTrigger 配置（滚动控制）：**
```javascript
scrollTrigger: {
  trigger: element,
  start: 'top 85%',
  end: 'top 25%',
  scrub: 1,           // 滚动控制进度
  toggleActions: 'play none none reverse'
}
```

**标准 ScrollTrigger 配置（触发动画）：**
```javascript
scrollTrigger: {
  trigger: element,
  start: 'top 85%',
  toggleActions: 'play none none reverse'
}
```

**3D 透视配置：**
```css
.perspective-container {
  perspective: 2000px;
  transform-style: preserve-3d;
}

.card-3d {
  transform-style: preserve-3d;
  backface-visibility: hidden;
}
```

---

## 🚀 必须实现的功能

### 1. 导航栏功能
- 固定顶部，毛玻璃效果（`backdrop-filter: blur(20px)`）
- 滚动时添加阴影和压缩效果
- Logo 发光效果（box-shadow 霓虹光）
- 点击导航链接平滑滚动到对应区域
- 移动端隐藏导航链接（响应式）

**平滑滚动代码（必须实现）：**
```javascript
// 必须引入 ScrollToPlugin
gsap.registerPlugin(ScrollTrigger, ScrollToPlugin, MotionPathPlugin);

// 平滑滚动导航
document.querySelectorAll('.nav-link, .hero-button').forEach(link => {
  link.addEventListener('click', (e) => {
    e.preventDefault();
    const targetId = link.getAttribute('href');
    const target = document.querySelector(targetId);
    if (target) {
      const offset = 80; // 导航栏高度
      const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
      gsap.to(window, {
        duration: 1,
        scrollTo: targetPosition,
        ease: 'power3.inOut'
      });
    }
  });
});
```

### 2. 返回顶部按钮
- 滚动超过 500px 时显示
- 点击平滑滚动到顶部
- 悬停时发光 + 旋转效果
- 霓虹蓝色调

**返回顶部代码（必须实现）：**
```javascript
const backToTop = document.getElementById('backToTop');

// 监听滚动显示/隐藏按钮
window.addEventListener('scroll', () => {
  if (window.scrollY > 500) {
    backToTop.classList.add('visible');
  } else {
    backToTop.classList.remove('visible');
  }
});

// 点击返回顶部
backToTop.addEventListener('click', () => {
  gsap.to(window, {
    duration: 1,
    scrollTo: 0,
    ease: 'power3.inOut'
  });
});
```

### 3. 手机悬浮展示
- 3D 悬浮动画（持续上下浮动 + 轻微旋转）
- 鼠标跟随视角变化
- 霓虹光效环绕
- 粒子系统环绕手机

**手机悬浮代码：**
```javascript
// 手机悬浮动画
gsap.to('.phone-3d', {
  y: -20,
  rotation: 5,
  duration: 2,
  repeat: -1,
  yoyo: true,
  ease: 'sine.inOut'
});

// 鼠标跟随
const handlePhoneMouseMove = (event) => {
  const phone = document.querySelector('.phone-3d');
  const rect = phone.getBoundingClientRect();
  const x = ((event.clientX - rect.left) / rect.width - 0.5) * 20;
  const y = ((event.clientY - rect.top) / rect.height - 0.5) * 20;

  gsap.to(phone, {
    rotateY: x,
    rotateX: -y,
    duration: 0.5,
    ease: 'power2.out'
  });
};
```

### 4. 磁力卡片交互
- 鼠标靠近时卡片跟随移动
- 3D 旋转效果
- 霓虹光效增强
- 悬停时放大 + 发光

**磁力交互代码：**
```javascript
// 磁力效果
const handleMagneticMove = (event, item) => {
  const rect = item.getBoundingClientRect();
  const centerX = rect.left + rect.width / 2;
  const centerY = rect.top + rect.height / 2;
  const moveX = (event.clientX - centerX) / rect.width * 30;
  const moveY = (event.clientY - centerY) / rect.height * 30;

  gsap.to(item, {
    x: moveX,
    y: moveY,
    rotateX: moveY * -0.5,
    rotateY: moveX * 0.5,
    scale: 1.05,
    duration: 0.3,
    ease: 'power2.out'
  });

  // 霓虹光效
  gsap.to(item.querySelector('.neon-glow'), {
    opacity: 0.6,
    scale: 1.3,
    duration: 0.3
  });
};
```

### 5. 粒子背景系统
- 40-60 个科技蓝色调粒子
- 持续向上漂浮动画
- 随机大小、速度、延迟
- 固定定位，不影响滚动
- 部分粒子发光效果

**粒子系统代码：**
```javascript
// 创建粒子
function createParticles() {
  const container = document.getElementById('particles');
  const particleCount = 50;

  for (let i = 0; i < particleCount; i++) {
    const particle = document.createElement('div');
    particle.className = 'particle';
    particle.style.left = Math.random() * 100 + '%';
    particle.style.top = Math.random() * 100 + '%';
    particle.style.width = Math.random() * 4 + 1 + 'px';
    particle.style.height = particle.style.width;
    particle.style.opacity = Math.random() * 0.6 + 0.2;

    // 20% 粒子发光
    if (Math.random() > 0.8) {
      particle.style.boxShadow = '0 0 10px #3b82f6, 0 0 20px #3b82f6';
    }

    container.appendChild(particle);

    gsap.to(particle, {
      y: '-=100',
      x: `+=${Math.random() * 100 - 50}`,
      opacity: 0,
      duration: Math.random() * 4 + 3,
      repeat: -1,
      delay: Math.random() * 2,
      ease: 'none'
    });
  }
}
```

---

## 📁 交付要求

### 文件结构
**输出位置：** `page-demo/demo14/index.html`
**文件格式：** 单文件 HTML（包含所有 CSS、JS）

**文件结构：**
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>未来科技 | 智能手机官网</title>

  <!-- GSAP CDN -->
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollToPlugin.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/MotionPathPlugin.min.js"></script>

  <style>
    /* 所有 CSS 样式 */
    :root {
      --color-primary: #3b82f6;
      --color-dark: #0f172a;
      --color-light: #ffffff;
      --color-accent: #8b5cf6;
      --gradient-primary: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
      --gradient-neon: linear-gradient(90deg, #06b6d4, #3b82f6, #8b5cf6, #06b6d4);
    }
  </style>
</head>
<body>
  <!-- HTML 结构 -->
  <!-- 粒子层 -->
  <div class="particles-overlay" id="particles"></div>

  <!-- 导航栏 -->
  <nav class="navbar">...</nav>

  <!-- 首页 Hero -->
  <section class="hero-section">...</section>

  <!-- 其他区块 -->

  <!-- 返回顶部 -->
  <div class="back-to-top" id="backToTop">↑</div>

  <script>
    // 所有 JavaScript 代码
    gsap.registerPlugin(ScrollTrigger, ScrollToPlugin, MotionPathPlugin);

    // 所有动画函数
    function initAll() {
      createParticles();
      // ... 其他初始化
    }

    document.addEventListener('DOMContentLoaded', initAll);
  </script>
</body>
</html>
```

### 代码规范
**CSS 规范：**
- ✅ 使用 CSS 变量定义颜色和尺寸
- ✅ 移动端优先的响应式设计
- ✅ 使用 BEM 命名规范（推荐）
- ✅ 合理使用 CSS3 动画和过渡
- ✅ 添加 `will-change` 优化动画性能
- ✅ 使用 GPU 加速属性（transform, opacity, filter）

**JavaScript 规范：**
- ✅ 函数命名清晰（init + 功能名）
- ✅ 添加详细注释
- ✅ 避免全局变量污染
- ✅ 统一在 `DOMContentLoaded` 事件中初始化
- ✅ 事件监听器统一管理
- ✅ 使用 `gsap.context()` 管理动画实例（可选）

**动画规范：**
- ✅ 所有动画函数独立封装
- ✅ 统一在 `initAll()` 函数中调用
- ✅ 动画实例及时清理（页面卸载时）
- ✅ 使用 `ScrollTrigger.refresh()` 刷新触发器
- ✅ 合理使用 `stagger` 错峰动画
- ✅ 使用 `timeline` 组合复杂动画

---

## ⚠️ 关键注意事项

### 性能优化
1. ✅ 减少 DOM 操作，使用 documentFragment 批量插入
2. ✅ 使用 `will-change: transform, opacity` 提示浏览器优化
3. ✅ 避免在 scroll 事件中执行重排/重绘
4. ✅ 使用 `requestAnimationFrame` 优化动画帧
5. ✅ 粒子数量控制在 60 个以内
6. ✅ 使用 `transform: translateZ(0)` 开启 GPU 加速
7. ✅ 避免过多 `box-shadow` 影响性能
8. ✅ 图片懒加载配合 ScrollTrigger

### 兼容性
1. ✅ 支持 Chrome、Firefox、Safari、Edge 最新版本
2. ✅ iOS Safari 和 Android Chrome 完美适配
3. ✅ 添加 `-webkit-` 前缀支持旧版浏览器
4. ✅ 优雅降级，不支持动画时仍可正常浏览
5. ✅ 检测 `prefers-reduced-motion` 媒体查询

### 常见问题
**问题 1：内容不可见**
- ❌ 原因：`opacity: 0` 但动画未触发
- ✅ 解决：初始 `opacity: 1`，或确保动画一定触发

**问题 2：动画卡顿**
- ❌ 原因：同时执行过多动画或过于复杂的 box-shadow
- ✅ 解决：使用 `stagger` 错峰动画，减少粒子数量，简化阴影

**问题 3：滚动跳转失效**
- ❌ 原因：未引入 `ScrollToPlugin`
- ✅ 解决：添加 CDN 并注册插件

**问题 4：返回顶部不显示**
- ❌ 原因：CSS 类名不匹配
- ✅ 解决：确保 HTML 和 CSS 类名一致

**问题 5：MotionPath 不工作**
- ❌ 原因：未引入 `MotionPathPlugin`
- ✅ 解决：添加 CDN 并注册插件

**问题 6：3D 效果不生效**
- ❌ 原因：缺少 `perspective` 或 `transform-style: preserve-3d`
- ✅ 解决：添加透视容器和 3D 样式

---

## 🎯 创作流程

### 第一步：分析需求
1. ✅ 理解手机科技主题定位
2. ✅ 分析参考组件的动画逻辑
3. ✅ 规划页面结构和内容布局

### 第二步：设计视觉
1. ✅ 确定科技蓝配色方案
2. ✅ 查找合适的高质量手机图片
3. ✅ 设计动画交互方案（磁力、霓虹、3D）

### 第三步：编写代码
1. ✅ 搭建 HTML 结构（10 个区块）
2. ✅ 编写 CSS 样式（响应式 + 玻璃态 + 霓虹光效）
3. ✅ 实现 JavaScript 功能和动画（GSAP）
4. ✅ 引入必要的 CDN 库（GSAP + 插件）

### 第四步：测试优化
1. ✅ 测试滚动动画是否正常
2. ✅ 测试响应式适配
3. ✅ 测试性能（60fps）
4. ✅ 检查控制台错误
5. ✅ 测试各种交互效果

### 第五步：最终检查
1. ✅ 导航栏点击跳转
2. ✅ 返回顶部按钮
3. ✅ 所有动画触发正常
4. ✅ 磁力交互流畅
5. ✅ 霓虹光效明显
6. ✅ 3D 效果自然
7. ✅ 内容显示完整

---

## 📝 最终检查清单

在提交代码前，请确保：

### 功能检查
- [ ] 所有 GSAP 插件正确引入（ScrollTrigger + ScrollToPlugin + MotionPathPlugin）
- [ ] 所有元素初始 `opacity: 1` 或确保动画触发
- [ ] 导航栏点击可以跳转到对应区域
- [ ] 返回顶部按钮在滚动 500px 后显示
- [ ] 手机 3D 悬浮动画流畅
- [ ] 磁力卡片交互正常
- [ ] 霓虹光效明显
- [ ] MotionPath 路径运动流畅
- [ ] 图片对比功能正常
- [ ] 横向滚动平滑
- [ ] 无限滚动文字循环
- [ ] 卡片翻转效果自然
- [ ] 网格变形流畅

### 性能检查
- [ ] 页面滚动流畅，无卡顿（60fps）
- [ ] 粒子数量控制在 60 个以内
- [ ] 无控制台错误
- [ ] 图片使用 Unsplash 高质量图库
- [ ] 图片懒加载正常
- [ ] GPU 加速优化生效

### 视觉检查
- [ ] 科技蓝配色统一
- [ ] 玻璃态效果明显
- [ ] 霓虹光效不过度
- [ ] 3D 效果自然不眩晕
- [ ] 文字和图片搭配合理
- [ ] 响应式设计完美适配
- [ ] 移动端体验良好

### 代码检查
- [ ] 代码注释详细清晰
- [ ] 函数命名规范
- [ ] CSS 变量使用合理
- [ ] 动画函数独立封装
- [ ] 事件监听器统一管理

---

## 🎨 图片资源推荐

**Unsplash 搜索关键词：**
- 手机产品：`smartphone`, `iphone`, `android phone`, `mobile device`
- 科技感：`technology`, `futuristic`, `tech`, `innovation`
- 抽象科技：`abstract technology`, `digital art`, `neon`, `cyberpunk`
- 蓝色调：`blue technology`, `futuristic blue`, `digital blue`
- 背景素材：`gradient background`, `dark gradient`, `abstract lines`

**图片尺寸建议：**
- Hero 背景：1920x1080
- 手机产品：800x1200（竖版）
- 特性展示：1200x800（横版）
- 案例图片：800x600
- 装饰图片：根据需要调整

**手机产品图片建议：**
- 使用透明背景 PNG
- 多角度展示（正面、侧面、背面）
- 高分辨率（至少 2x）
- 真实产品照片或高质量 3D 渲染图

---

## 💡 创作建议

**让网站更专业：**
1. ✅ 首页要有记忆点，第一屏足够震撼
2. ✅ 使用 GSAP 的 `timeline` 组合复杂动画
3. ✅ 添加丰富的微交互（悬停、点击、拖拽）
4. ✅ 使用 `stagger` 实现错峰动画
5. ✅ 添加过渡效果，避免生硬切换
6. ✅ 霓虹光效适度，避免视觉疲劳
7. ✅ 3D 效果自然，避免眩晕
8. ✅ 磁力交互流畅，响应及时

**避免常见错误：**
1. ❌ 不要过度使用动画，影响性能
2. ❌ 不要让动画遮挡内容
3. ❌ 不要忘记响应式适配
4. ❌ 不要使用过大的图片影响加载
5. ❌ 不要忽略移动端体验
6. ❌ 不要过度使用霓虹光效
7. ❌ 不要让 3D 效果过于剧烈
8. ❌ 不要忽略无障碍访问

**动画节奏控制：**
- Hero 区域：慢速震撼（2-3 秒）
- 特性卡片：中速交互（0.5-1 秒）
- 数据展示：中等节奏（1-2 秒）
- 滚动动画：跟随滚动（scrub: 1）
- 微交互：快速响应（0.2-0.4 秒）

---

**请按照以上要求，创作一个专业、震撼、流畅的手机科技产品官网！**

**输出文件：** `page-demo/demo14/index.html`
