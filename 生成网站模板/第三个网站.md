你是一位专业的前端开发工程师、UI/UX设计师和动画专家，精通以下技术栈：

**核心技术栈：**
- 前端框架：HTML5 + JavaScript ES6+（纯原生实现）
- 动画引擎：GSAP 3.12+（含 ScrollTrigger + ScrollToPlugin）
- 构建工具：无需构建工具，单文件 HTML 部署
- 样式方案：CSS3 + Flexbox + Grid + CSS Variables
- 图片资源：Unsplash（高质量免费图库）

**你的核心能力：**
✅ 创建流畅的 60fps 动画效果
✅ 精通 GSAP 的 ScrollTrigger 滚动控制
✅ 擅长响应式设计和性能优化
✅ 注重用户体验和视觉设计
✅ GPU 加速优化，避免性能瓶颈
✅ 无障碍访问和 SEO 友好

---

## 📋 项目要求

### 主题定位
创建一个以「水墨中国风」为主题的文化艺术公司官网，融合传统美学与现代科技感。

### 页面结构
1. **导航栏** - 固定顶部，Logo + 导航链接，滚动时样式变化
2. **首页** - 企业核心理念展示，震撼视觉冲击力
3. **企业理念** - 3-4 个核心理念卡片展示
4. **产品介绍** - 6 个产品卡片，图文并茂
5. **服务展示** - 4 个服务项目详细介绍
6. **数据统计** - 4 项关键数据动态展示
7. **客户案例** - 5 个精选案例横向滚动
8. **联系我们** - 联系信息 + 在线表单
9. **页脚** - 完整的网站导航和版权信息

### 底层动画模板（必须使用）
**参考位置：** `pan-gsap-peizhi-20260209/src/views/template-read`
**核心动画元素：**
1. 粒子波浪动画系统（30-50 个浮动粒子）
2. 3 层水墨波浪背景动画
3. 面板波浪式入场动画（y + scale + rotation）
4. 持续循环的背景动画

**重要提示：**
- ❌ 不能直接引入 template-read 文件
- ✅ 复制代码并根据需求优化调整
- ✅ 保持核心动画逻辑，优化视觉效果
- ✅ 所有动画必须服务于内容

### 参考组件动画效果
**参考位置：** `pan-gsap-peizhi-20260209/src/views/web`
**需参考的组件（复制并优化）：**
1. `ScrollInkDiffusion` - 水墨扩散效果（用于背景）
2. `ScrollMagneticGrid` - 磁力网格效果（用于产品展示）
3. `ScrollMinimalistCards` - 极简卡片动画（用于理念展示）
4. `ScrollModernCards` - 现代卡片效果（用于服务展示）
5. `ScrollMorphingCards` - 变形卡片效果（用于案例展示）
6. `HorizontalScroll` - 横向滚动效果（用于案例区域）
7. `InfiniteScrollText` - 无限滚动文字（用于标题装饰）
8. `LiquidWave` - 液体波浪动画（用于背景装饰）
9. `MorphGrid` - 变形网格效果（用于产品网格）
10. `MotionPathCards` - 运动路径卡片（用于服务卡片）

**重要提示：**
- ❌ 不能直接引用这些 Vue 组件
- ✅ 提取动画逻辑，用原生 JS + GSAP 重写
- ✅ 保持动画效果的真实性和流畅度
- ✅ 根据页面需求优化动画参数

---

## 🎨 设计要求

### 视觉风格
**整体风格：** 大气简约 + 水墨意境 + 现代科技
**配色方案：**
- 主色调：中国红 (#c41e3a)
- 辅助色：深灰黑 (#1a1a1a)
- 背景色：米白色 (#f5f3ef) + 深色区域交替
- 点缀色：金棕色、墨绿色

**排版原则：**
- ✅ 文字和图片搭配合理，避免过于密集
- ✅ 段落文字不超过 3 行，保持简洁
- ✅ 使用高质量 Unsplash 图片
- ✅ 字体：标题用宋体系列，正文用黑体/微软雅黑

### 动画设计原则
**必须遵循：**
1. ✅ 所有元素初始 `opacity: 1`，避免内容不可见
2. ✅ 滚动触发动画 + 持续动画结合
3. ✅ 使用 `toggleActions: 'play none none reverse'` 实现反向播放
4. ✅ 离开视窗时自动反向播放动画
5. ✅ 使用 GPU 加速属性（transform, will-change）
6. ✅ 避免动画影响页面滚动性能

**标准 ScrollTrigger 配置：**
```javascript
scrollTrigger: {
  trigger: element,
  start: 'top 85%',
  end: 'top 25%',
  scrub: 1,           // 滚动控制进度
  toggleActions: 'play none none reverse'
}
```

**无 ScrollTrigger 配置：**
```javascript
scrollTrigger: {
  trigger: element,
  start: 'top 85%',
  toggleActions: 'play none none reverse'
}
```

---

## 🚀 必须实现的功能

### 1. 导航栏功能
- 固定顶部，背景毛玻璃效果（`backdrop-filter: blur(10px)`）
- 滚动时添加阴影和压缩效果
- 点击导航链接平滑滚动到对应区域
- 移动端隐藏导航链接（响应式）

**平滑滚动代码（必须实现）：**
```javascript
// 必须引入 ScrollToPlugin
gsap.registerPlugin(ScrollTrigger, ScrollToPlugin);

// 平滑滚动导航
document.querySelectorAll('.nav-link, .hero-button').forEach(link => {
  link.addEventListener('click', (e) => {
    e.preventDefault();
    const targetId = link.getAttribute('href');
    const target = document.querySelector(targetId);
    if (target) {
      const offset = 80; // 导航栏高度
      const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - offset;
      gsap.to(window, {
        duration: 1,
        scrollTo: targetPosition,
        ease: 'power3.inOut'
      });
    }
  });
});
```

### 2. 返回顶部按钮
- 滚动超过 500px 时显示
- 点击平滑滚动到顶部
- 悬停时放大效果

**返回顶部代码（必须实现）：**
```javascript
const backToTop = document.getElementById('backToTop');

// 监听滚动显示/隐藏按钮
window.addEventListener('scroll', () => {
  if (window.scrollY > 500) {
    backToTop.classList.add('visible');
  } else {
    backToTop.classList.remove('visible');
  }
});

// 点击返回顶部
backToTop.addEventListener('click', () => {
  gsap.to(window, {
    duration: 1,
    scrollTo: 0,
    ease: 'power3.inOut'
  });
});
```

### 3. 数据统计动画
- 数字从 0 递增到目标值
- 卡片入场动画（scale + y + opacity）
- 滚动到区域触发动画
- 离开区域反向播放

### 4. 粒子背景系统
- 30-50 个浮动粒子
- 持续向上漂浮动画
- 随机大小、速度、延迟
- 固定定位，不影响滚动

---

## 📁 交付要求

### 文件结构
**输出位置：** `page-demo/demo12/index.html`
**文件格式：** 单文件 HTML（包含所有 CSS、JS）

**文件结构：**
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>网站标题</title>
  
  <!-- GSAP CDN -->
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/gsap.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollTrigger.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/gsap@3.12.5/dist/ScrollToPlugin.min.js"></script>
  
  <style>
    /* 所有 CSS 样式 */
  </style>
</head>
<body>
  <!-- HTML 结构 -->
  
  <script>
    // 所有 JavaScript 代码
  </script>
</body>
</html>
```

### 代码规范
**CSS 规范：**
- ✅ 使用 CSS 变量定义颜色和尺寸
- ✅ 移动端优先的响应式设计
- ✅ 使用 BEM 命名规范（可选）
- ✅ 合理使用 CSS3 动画和过渡

**JavaScript 规范：**
- ✅ 函数命名清晰（init + 功能名）
- ✅ 添加详细注释
- ✅ 避免全局变量污染
- ✅ 统一在 `DOMContentLoaded` 事件中初始化

**动画规范：**
- ✅ 所有动画函数独立封装
- ✅ 统一在 `initAll()` 函数中调用
- ✅ 动画实例及时清理（onUnmounted）
- ✅ 使用 `ScrollTrigger.refresh()` 刷新触发器

---

## ⚠️ 关键注意事项

### 性能优化
1. ✅ 减少 DOM 操作，使用 documentFragment 批量插入
2. ✅ 使用 `will-change` 提示浏览器优化
3. ✅ 避免在 scroll 事件中执行重排/重绘
4. ✅ 使用 `requestAnimationFrame` 优化动画帧
5. ✅ 粒子数量控制在 50 个以内

### 兼容性
1. ✅ 支持 Chrome、Firefox、Safari、Edge 最新版本
2. ✅ iOS Safari 和 Android Chrome 完美适配
3. ✅ 添加 `-webkit-` 前缀支持旧版浏览器
4. ✅ 优雅降级，不支持动画时仍可正常浏览

### 常见问题
**问题 1：内容不可见**
- ❌ 原因：`opacity: 0` 但动画未触发
- ✅ 解决：初始 `opacity: 1`，或确保动画一定触发

**问题 2：动画卡顿**
- ❌ 原因：同时执行过多动画
- ✅ 解决：使用 `stagger` 错峰动画，减少粒子数量

**问题 3：滚动跳转失效**
- ❌ 原因：未引入 `ScrollToPlugin`
- ✅ 解决：添加 CDN 并注册插件

**问题 4：返回顶部不显示**
- ❌ 原因：CSS 类名不匹配
- ✅ 解决：确保 HTML 和 CSS 类名一致

---

## 🎯 创作流程

### 第一步：分析需求
1. ✅ 理解水墨中国风主题定位
2. ✅ 分析参考组件的动画逻辑
3. ✅ 规划页面结构和内容布局

### 第二步：设计视觉
1. ✅ 确定配色方案
2. ✅ 查找合适的高质量图片
3. ✅ 设计动画交互方案

### 第三步：编写代码
1. ✅ 搭建 HTML 结构
2. ✅ 编写 CSS 样式（响应式）
3. ✅ 实现 JavaScript 功能和动画
4. ✅ 引入必要的 CDN 库

### 第四步：测试优化
1. ✅ 测试滚动动画是否正常
2. ✅ 测试响应式适配
3. ✅ 测试性能（60fps）
4. ✅ 检查控制台错误

### 第五步：最终检查
1. ✅ 导航栏点击跳转
2. ✅ 返回顶部按钮
3. ✅ 所有动画触发正常
4. ✅ 内容显示完整

---

## 📝 最终检查清单

在提交代码前，请确保：

- [ ] 所有 GSAP 插件正确引入（ScrollTrigger + ScrollToPlugin）
- [ ] 所有元素初始 `opacity: 1` 或确保动画触发
- [ ] 导航栏点击可以跳转到对应区域
- [ ] 返回顶部按钮在滚动 500px 后显示
- [ ] 数据统计数字从 0 递增到目标值
- [ ] 所有卡片有入场动画
- [ ] 粒子背景正常浮动
- [ ] 响应式设计在移动端正常显示
- [ ] 无控制台错误
- [ ] 页面滚动流畅，无卡顿
- [ ] 图片使用 Unsplash 高质量图库
- [ ] 文字和图片搭配合理
- [ ] 代码注释详细清晰

---

## 🎨 图片资源推荐

**Unsplash 搜索关键词：**
- 水墨画：`chinese ink painting`, `watercolor landscape`, `traditional art`
- 山水：`mountain landscape`, `riverscape`, `traditional scenery`
- 书法：`chinese calligraphy`, `brush writing`, `ink art`
- 陶瓷：`chinese ceramics`, `traditional pottery`, `tea ceremony`
- 园林：`chinese garden`, `traditional architecture`, `temple`
- 文化：`asian culture`, `eastern aesthetics`, `traditional crafts`

**图片尺寸建议：**
- Hero 背景：1920x1080
- 产品图片：800x600
- 案例图片：800x600
- 装饰图片：根据需要调整

---

## 💡 创作建议

**让网站更专业：**
1. ✅ 首页要有记忆点，第一屏足够震撼
2. ✅ 使用 GSAP 的 `timeline` 组合动画
3. ✅ 添加微交互（悬停、点击反馈）
4. ✅ 使用 `stagger` 实现错峰动画
5. ✅ 添加过渡效果，避免生硬切换

**避免常见错误：**
1. ❌ 不要过度使用动画，影响性能
2. ❌ 不要让动画遮挡内容
3. ❌ 不要忘记响应式适配
4. ❌ 不要使用过大的图片影响加载
5. ❌ 不要忽略移动端体验

---

**请按照以上要求，创作一个专业、大气、流畅的水墨中国风文化公司官网！**

**输出文件：** `page-demo/demo12/index.html`
