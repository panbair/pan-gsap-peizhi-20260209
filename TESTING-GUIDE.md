# Anim-Card 测试指南

## 快速测试

### 方法 1：在项目中测试
```bash
cd d:/work20240226/rcs-20250311/gsap-20260209
npm run dev
```

访问：`http://localhost:5173/preview`

### 方法 2：打开测试页面
直接在浏览器中打开：
```
d:/work20240226/rcs-20250311/gsap-20260209/test-anim-card.html
```

---

## 测试要点

### ✅ 1. 滚动触发
- [ ] 卡片滚动到视窗 85% 位置时自动触发动画
- [ ] 每张卡片只触发一次
- [ ] 滚出视窗再滚回不会重复触发

### ✅ 2. 动画独立性
- [ ] 每张卡片使用自己配置的动画
- [ ] 不同卡片的动画互不影响
- [ ] 卡片右上角有动画类型指示器

### ✅ 3. 交错效果
- [ ] 同一行的卡片依次播放（0.08s 间隔）
- [ ] 不同行的卡片独立触发
- [ ] 动画流畅，无卡顿

### ✅ 4. 点击重播
- [ ] 点击卡片立即重播动画
- [ ] 重播时使用相同的动画配置
- [ ] 重播时不影响其他卡片

### ✅ 5. 视觉反馈
- [ ] Hover 时卡片上移 4px
- [ ] Active 时卡片上移 2px
- [ ] 有阴影效果
- [ ] 右上角显示动画类型（绿/橙/蓝）

### ✅ 6. 控制台日志
- [ ] 播放动画时有日志输出
- [ ] 重播动画时有日志输出
- [ ] 动画配置不存在时有警告

---

## 动画类型说明

### 🟢 Timeline（绿色）
- 多阶段时间线动画
- 例如：hologram, neonGlitch, starTwinkle

### 🟠 FromTo（橙色）
- 从某状态到某状态的动画
- 例如：scaleIn, rotateIn, bounceIn

### 🔵 To（蓝色）
- 从当前状态到目标状态的动画
- 例如：fadeIn, shake, pulse

---

## 调试步骤

### 1. 打开控制台
按 `F12` 或右键 → 检查 → Console

### 2. 滚动页面
观察控制台输出：
```
播放动画: fadeIn
播放动画: scaleIn
播放动画: rotateIn
...
```

### 3. 点击卡片重播
观察控制台输出：
```
重播动画: fadeIn
```

### 4. 检查警告
如果动画配置不存在，会看到：
```
警告: 动画配置不存在: xxx
警告: 卡片缺少 data-anim 属性
```

---

## 常见问题排查

### 问题 1：卡片不显示动画

**检查清单**：
- [ ] 卡片的 `data-anim` 属性是否存在
- [ ] 动画配置是否存在于 `config.js` 中
- [ ] 控制台是否有警告信息

**解决方法**：
```javascript
// 检查 data-anim
console.log(el.dataset.anim)

// 检查配置
console.log(animConfig[el.dataset.anim])
```

---

### 问题 2：动画重复触发

**检查清单**：
- [ ] `once: true` 是否设置
- [ ] ScrollTrigger 是否正确清理

**解决方法**：
```javascript
// 确保只触发一次
once: true,
toggleActions: 'play none none none'
```

---

### 问题 3：动画不流畅

**检查清单**：
- [ ] 同时播放的动画数量
- [ ] 动画配置复杂度
- [ ] 设备性能

**解决方法**：
```javascript
// 增加交错延迟
const delay = (i % 4) * 0.12  // 从 0.08 增加到 0.12

// 或减少同一行显示的卡片数
grid-template-columns: repeat(auto-fill, minmax(180px, 1fr))  // 增加 minmax
```

---

### 问题 4：点击重播不生效

**检查清单**：
- [ ] 动画配置是否存在
- [ ] 点击事件是否绑定
- [ ] 控制台是否有错误

**解决方法**：
```javascript
// 检查动画配置
console.log(animConfig[animName])

// 检查点击事件
el.addEventListener('click', () => console.log('clicked'))
```

---

## 性能测试

### 1. 帧率监控
打开 Chrome DevTools → Performance → 录制

**目标**：
- 帧率 ≥ 60fps
- 无卡顿
- CPU 使用率合理

### 2. 内存监控
打开 Chrome DevTools → Memory → 录制

**目标**：
- 无内存泄漏
- 内存使用稳定
- ScrollTrigger 正确清理

### 3. 滚动性能
快速滚动页面，观察：
- [ ] 动画是否及时触发
- [ ] 是否有延迟
- [ ] 是否有跳帧

---

## 兼容性测试

### 浏览器版本
- ✅ Chrome 87+
- ✅ Firefox 78+
- ✅ Safari 14+
- ✅ Edge 88+

### 测试设备
- 💻 桌面端（1920x1080）
- 💻 桌面端（1366x768）
- 📱 平板（768x1024）
- 📱 手机（375x667）

---

## 测试报告模板

```markdown
## 测试报告

**测试日期**: 2025-02-26
**测试人员**: xxx
**测试环境**: Chrome 120 / Windows 11

### 功能测试
- [x] 滚动触发
- [x] 动画独立性
- [x] 交错效果
- [x] 点击重播
- [x] 视觉反馈
- [x] 控制台日志

### 性能测试
- 帧率: 60fps
- 内存使用: 正常
- CPU 使用: 正常

### 兼容性测试
- Chrome: ✅
- Firefox: ✅
- Safari: ✅
- Edge: ✅

### 问题记录
无

### 结论
通过所有测试，功能正常，性能良好。
```

---

## 反馈和建议

如果发现问题或有改进建议，请：

1. 记录详细的问题描述
2. 附上控制台日志
3. 说明复现步骤
4. 提供浏览器和设备信息

---

**测试指南版本**: v1.0
**最后更新**: 2025-02-26
